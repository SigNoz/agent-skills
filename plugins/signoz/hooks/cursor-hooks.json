{
  "description": "Nudge users to finish SigNoz MCP setup at session start while the bundled endpoint is still the not-setup placeholder",
  "hooks": {
    "sessionStart": [
      {
        "command": "./hooks/scripts/self-hosted-nudge-cursor.js"
      }
    ]
  }
}
